import FullHeightLayout from '@/components/layout/FullHeightLayout';
import { setLayout } from '@/utils/layout';

const FindPasswordPage = () => {
  return <p>비밀번호 찾기</p>;
};

setLayout(FindPasswordPage, FullHeightLayout);

export default FindPasswordPage;
